# Flask API base URL (used in production or when not using proxy)
VITE_API_BASE_URL=/v1

# Dev server proxy target (Flask server URL)
VITE_PROXY_TARGET=http://localhost:5000

# API prefix/path that Vite proxies to Flask
VITE_API_PREFIX=/v1

